Classroom Info
<p>
<%= render partial: 'classroom', locals: {classroom: @classroom} %></p>

All students in class:
<% @classroom.students.each do |student| %>
  <%= render partial: 'students/student', locals: {student: student} %>
<% end %>